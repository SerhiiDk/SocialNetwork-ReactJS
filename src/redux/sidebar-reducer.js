


let initialState = {
    friends: [
        { id: 101, name: "Alex" },
        { id: 102, name: "Maria" },
        { id: 103, name: "John" },
    ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;